> ## 文档索引
> 获取完整文档索引：https://code.claude.com/docs/llms.txt
> 使用此文件发现所有可用页面，然后再进一步探索。

# 第 13 周 · 2026 年 3 月 23–27 日

> Auto 模式实现免提权限管理、内置计算机使用、云端 PR 自动修复、对话记录搜索，以及适用于 Windows 的 PowerShell 工具。

<div className="digest-meta">
  <span>发布版本 <a href="/docs/en/changelog#2-1-83">v2.1.83 → v2.1.85</a></span>
  <span>6 项功能 · 3 月 23–27 日</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Auto 模式</span>
    <span className="digest-feature-pill">研究预览</span>
  </div>

  <p className="digest-feature-lede">Auto 模式将权限提示词交给分类器处理。安全的编辑和命令会在不打断你的情况下执行；任何具有破坏性或可疑的操作会被阻止并提醒。这是在每次审批文件写入和使用 <code>--dangerously-skip-permissions</code> 运行之间的折中方案。</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/auto-mode.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=367c9e9d4ba5bc57ec4b935154bf1fbb" alt="Claude Code 提示词底部显示黄色的 'auto mode on (shift+tab to cycle)' 指示器" width="2400" height="691" data-path="images/whats-new/auto-mode.png" />
  </Frame>

  <p className="digest-feature-try">使用 Shift+Tab 切换到 Auto 模式，或将其设为默认模式：</p>

  ```json ~/.claude/settings.json {3}
  {
    "permissions": {
      "defaultMode": "auto"
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/en/permission-modes">权限模式指南</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">计算机使用</span>
    <span className="digest-feature-pill">桌面端</span>
  </div>

  <p className="digest-feature-lede">Claude 现在可以从 Claude Code 桌面应用控制你的实际桌面：打开原生应用、操作 iOS 模拟器、驱动硬件控制面板，并在屏幕上验证更改。默认关闭，每次操作前会请求确认。最适合处理其他方式无法触及的场景：没有 API 的应用、专有工具、任何只以 GUI 形式存在的东西。</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/computer-use.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=d631de2017edafff463505f8ddbc0f51" alt="Claude 桌面端设置中启用了计算机使用开关，显示允许 Claude 在你授权的应用中截屏和控制键盘鼠标的选项" width="2376" height="1210" data-path="images/whats-new/computer-use.png" />
  </Frame>

  <p className="digest-feature-try">在设置中启用该功能，授予操作系统权限，然后让 Claude 端到端验证一项更改：</p>

  ```text Claude Code
  > Open the iOS simulator, tap through the onboarding flow, and screenshot each step
  ```

  <a className="digest-feature-link" href="/docs/en/desktop#let-claude-use-your-computer">计算机使用指南</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">PR 自动修复</span>
    <span className="digest-feature-pill">Web 端</span>
  </div>

  <p className="digest-feature-lede">打开 PR 时打开开关，然后就可以走开了。Claude 会监控 CI、修复失败、处理代码评审意见，并持续推送直到变绿。再也不用盯着一个 PR 经历六轮 lint 错误了。</p>

  <Frame>
    <img src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/auto-fix.png?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=c62b181c6c5d96929f0b43525f9f3584" alt="Claude Code Web 端 CI 面板显示 Auto fix 开关已启用，描述为 'Proactively fix CI failures and review comments'" width="960" height="444" data-path="images/whats-new/auto-fix.png" />
  </Frame>

  <p className="digest-feature-try">在 Claude Code Web 端创建 PR 后，在 CI 面板中切换 Auto fix。</p>

  <a className="digest-feature-link" href="/docs/en/claude-code-on-the-web#auto-fix-pull-requests">自动修复 Pull Request</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">对话记录搜索</span>
    <span className="digest-feature-pill">v2.1.83</span>
  </div>

  <p className="digest-feature-lede">在对话记录模式下按 <code>/</code> 搜索你的对话内容。<code>n</code> 和 <code>N</code> 逐个跳转匹配项。终于可以找到 Claude 在 400 条消息前运行的那条 Bash 命令了。</p>

  <p className="digest-feature-try">打开对话记录模式并搜索：</p>

  ```text Claude Code
  Ctrl+O    # 打开对话记录
  /migrate  # 搜索 "migrate"
  n         # 下一个匹配
  N         # 上一个匹配
  ```

  <a className="digest-feature-link" href="/docs/en/fullscreen#search-and-review-the-conversation">全屏模式指南</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">PowerShell 工具</span>
    <span className="digest-feature-pill">预览</span>
    <span className="digest-feature-pill">v2.1.84</span>
  </div>

  <p className="digest-feature-lede">Windows 端新增了与 Bash 并列的原生 PowerShell 工具。Claude 可以运行 cmdlet、管道传输对象，并使用 Windows 原生路径，无需通过 Git Bash 进行转换。</p>

  <p className="digest-feature-try">在设置中开启：</p>

  ```json .claude/settings.json {3}
  {
    "env": {
      "CLAUDE_CODE_USE_POWERSHELL_TOOL": "1"
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/en/tools-reference#powershell-tool">PowerShell 工具文档</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">条件钩子</span>
    <span className="digest-feature-pill">v2.1.85</span>
  </div>

  <p className="digest-feature-lede">钩子现在可以使用权限规则语法声明 <code>if</code> 字段。你的 pre-commit 检查仅在 <code>Bash(git commit \*)</code> 时触发，而不是每次 bash 调用，从而减少繁忙会话中的进程开销。</p>

  <p className="digest-feature-try">将钩子限定为仅 git 提交时触发：</p>

  ```json .claude/settings.json {5}
  {
    "hooks": {
      "PreToolUse": [{
        "hooks": [{
          "if": "Bash(git commit *)",
          "type": "command",
          "command": ".claude/hooks/lint-staged.sh"
        }]
      }]
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/en/hooks">钩子参考文档</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">其他改进</p>

  <div className="digest-wins-grid">
    <div>插件 <code>userConfig</code> 现已公开：启用时提示设置，支持钥匙串加密存储</div>
    <div>粘贴的图片会插入 <code>\[Image #N]</code> 标记，可按位置引用</div>
    <div><code>managed-settings.d/</code> 目录支持分层策略片段</div>
    <div><code>CwdChanged</code> 和 <code>FileChanged</code> 钩子事件，适用于 direnv 风格的设置</div>
    <div>代理可在 frontmatter 中声明 <code>initialPrompt</code> 以自动提交第一轮</div>
    <div><code>Ctrl+X Ctrl+E</code> 打开外部编辑器，与 readline 行为一致</div>
    <div>在任何响应之前中断会自动恢复你的输入</div>
    <div><code>/status</code> 现在在 Claude 响应期间也可使用</div>
    <div>深度链接在你首选的终端中打开，而非第一个检测到的终端</div>
    <div>空闲 75 分钟以上后提示 <code>/clear</code></div>
    <div>VS Code：速率限制横幅、双击 Esc 撤销选择器</div>
  </div>
</div>

[v2.1.83–v2.1.85 完整更新日志 →](/zh/changelog#2-1-83)
