> ## 文档索引
> 在此获取完整文档索引：https://code.claude.com/docs/llms.txt
> 使用此文件发现所有可用页面，然后再进一步探索。

# 第 14 周 · 2026 年 3 月 30 日 – 4 月 3 日

> CLI 中的计算机操控、交互式产品内教程、无闪烁渲染、按工具的 MCP 结果大小覆盖，以及 PATH 上的插件可执行文件。

<div className="digest-meta">
  <span>版本 <a href="/docs/en/changelog#2-1-86">v2.1.86 → v2.1.91</a></span>
  <span>5 项功能 · 3 月 30 日 – 4 月 3 日</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">CLI 中的计算机操控</span>
    <span className="digest-feature-pill">研究预览</span>
  </div>

  <p className="digest-feature-lede">上周计算机操控在桌面应用中上线。本周它来到了 CLI：Claude 可以打开原生应用、点击 UI、测试自己的修改并修复问题，所有操作都在终端中完成。Web 应用已有验证循环；原生 iOS、macOS 和其他纯 GUI 应用则没有。现在它们有了。最适合在没有 API 可调用的应用和工具上完成闭环。仍处于早期阶段；可能会有一些粗糙之处。</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/cli-computer-use.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=c17a337902308d7c9121013ded0494db" data-path="images/whats-new/cli-computer-use.mp4" />
  </Frame>

  <p className="digest-feature-try">运行 <code>/mcp</code>，找到 <code>computer-use</code>，然后开启它。接着让 Claude 端到端验证一个更改：</p>

  ```text Claude Code
  > Open the iOS simulator, tap through onboarding, and screenshot each step
  ```

  <a className="digest-feature-link" href="/docs/en/computer-use">计算机操控指南</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">/powerup</span>
    <span className="digest-feature-pill">v2.1.90</span>
  </div>

  <p className="digest-feature-lede">通过动画演示在终端内交互式教授 Claude Code 功能的教程。Claude Code 更新频繁，上个月本可以改变你工作方式的功能可能就这样错过了。运行一次 <code>/powerup</code>，你就能了解有哪些功能。</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/powerup.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=fb88beddc0ecc8029da5ab029e4b28f1" data-path="images/whats-new/powerup.mp4" />
  </Frame>

  <p className="digest-feature-try">运行它：</p>

  ```text Claude Code
  > /powerup
  ```

  <a className="digest-feature-link" href="/docs/en/commands">命令参考</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">无闪烁渲染</span>
    <span className="digest-feature-pill">v2.1.89</span>
  </div>

  <p className="digest-feature-lede">选择启用带有虚拟化滚动回退的新替代屏幕渲染器。提示词输入固定在底部，鼠标选择可在长对话中正常工作，重绘时的闪烁消失了。取消设置 <code>CLAUDE\_CODE\_NO\_FLICKER</code> 可回退。</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/CfffsX01JHFnIKvD/images/whats-new/flicker-free.mp4?fit=max&auto=format&n=CfffsX01JHFnIKvD&q=85&s=7719e35e52a3f9734b0cf69edac333ad" data-path="images/whats-new/flicker-free.mp4" />
  </Frame>

  <p className="digest-feature-try">设置环境变量并重启 Claude Code：</p>

  ```bash
  export CLAUDE_CODE_NO_FLICKER=1
  claude
  ```

  <a className="digest-feature-link" href="/docs/en/fullscreen">全屏渲染</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">MCP 结果大小覆盖</span>
    <span className="digest-feature-pill">v2.1.91</span>
  </div>

  <p className="digest-feature-lede">MCP 服务器作者现在可以通过在工具的 <code>tools/list</code> 条目中设置 <code>anthropic/maxResultSizeChars</code> 来提高特定工具的截断上限，最高可达 500K 字符的硬性上限。之前上限是全局的，因此偶尔返回固有大型负载（如数据库架构或完整文件树）的工具会触及默认限制并被持久化到磁盘并附带文件引用。按工具的覆盖可以在工具真正需要时将这些结果保持内联。</p>

  <p className="digest-feature-try">在服务器的 <code>tools/list</code> 响应中标注该工具：</p>

  ```json highlight={5}
  {
    "name": "get_schema",
    "description": "Returns the full database schema",
    "_meta": {
      "anthropic/maxResultSizeChars": 500000
    }
  }
  ```

  <a className="digest-feature-link" href="/docs/en/mcp#raise-the-limit-for-a-specific-tool">MCP 参考</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">PATH 上的插件可执行文件</span>
    <span className="digest-feature-pill">v2.1.91</span>
  </div>

  <p className="digest-feature-lede">在插件根目录的 <code>bin/</code> 目录中放置可执行文件，Claude Code 会在插件启用期间将该目录添加到 Bash 工具的 <code>PATH</code> 中。Claude 随后可以在任何 Bash 工具调用中将该二进制文件作为裸命令调用，无需绝对路径或包装脚本。非常适合将 CLI 辅助工具与调用它们的命令、代理和钩子打包在一起。</p>

  <p className="digest-feature-try">在插件根目录添加 <code>bin/</code> 目录：</p>

  ```text highlight={4, 5}
  my-plugin/
  ├── .claude-plugin/
  │   └── plugin.json
  └── bin/
      └── my-tool
  ```

  <a className="digest-feature-link" href="/docs/en/plugins-reference#file-locations-reference">插件参考</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">其他改进</p>

  <div className="digest-wins-grid">
    <div>自动模式后续操作：新的 <code>PermissionDenied</code> 钩子在分类器拒绝时触发（返回 <code>retry: true</code> 让 Claude 尝试不同的方法），<code>/permissions</code> → 最近记录让你可以用 <code>r</code> 手动重试</div>
    <div><code>PreToolUse</code> 钩子中 <code>permissionDecision</code> 新增 <code>defer</code> 值：<code>-p</code> 会话在工具调用处暂停并以 <code>deferred\_tool\_use</code> 负载退出，以便 SDK 应用或自定义 UI 可以展示它，然后用 <code>--resume</code> 恢复</div>
    <div><code>/buddy</code>：孵化一只小生物观看你编程（4 月 1 日）</div>
    <div><code>disableSkillShellExecution</code> 设置阻止技能、斜杠命令和插件命令中的内联 shell</div>
    <div>编辑工具现在可以对通过 <code>cat</code> 或 <code>sed -n</code> 查看的文件生效，无需单独的 Read</div>
    <div>钩子输出超过 50K 时保存到磁盘，附带路径和预览，而非注入到上下文中</div>
    <div>交互式会话中思考摘要默认关闭（设置 <code>showThinkingSummaries: true</code> 可恢复）</div>
    <div>语音模式：按键说话修饰键组合、Windows WebSocket、macOS Apple Silicon 麦克风权限</div>
    <div><code>claude-cli://</code> 深链接支持多行提示词（编码为 <code>%0A</code>）</div>
  </div>
</div>

[v2.1.86–v2.1.91 完整更新日志 →](/zh/changelog#2-1-86)
